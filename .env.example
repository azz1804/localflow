# Copy this file to .env and fill your API key.
OPENAI_API_KEY=sk-your-key

# Audio transcription.
TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
TRANSCRIPTION_LANGUAGE=fr

# AI writing modes. They are mutually exclusive in the app.
ENABLE_PROMPT_MODE=false
ENABLE_MAIL_MODE=false
PROMPT_MODEL=gpt-5.6-luna

# Hold-to-talk hotkeys. "fn" is attempted first; fallback works better on many Macs.
HOLD_HOTKEY=fn
FALLBACK_HOLD_HOTKEY=option+space

# Toggle recording hotkey.
TOGGLE_HOTKEY=fn+space

# Local data.
HISTORY_RETENTION_DAYS=0
RESTORE_CLIPBOARD_AFTER_PASTE=true
PASTE_RESTORE_DELAY_MS=900
PREFER_BUILT_IN_MIC_FOR_BLUETOOTH=true

# Optional hands-free control. Keeps the built-in microphone listening while
# LocalFlow is idle; two hand claps start or finish a dictation.
ENABLE_DOUBLE_CLAP_CONTROL=false
